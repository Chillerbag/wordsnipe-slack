export default class Constants {
  static readonly WORD_SENT_SCORE = 3;
  static readonly WORD_SNIPED_SCORE = 10;
  static readonly GOT_SNIPED_SCORE = -5;
  static readonly SNIPE_FAILURE_SCORE = -3;
}